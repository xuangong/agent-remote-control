/** Stable app geometry while a returning browser checks access and loads code. */
export function WorkspaceShell() {
  return <div className="gateway-workspace-shell" aria-label="Workspace loading"><div /><main /><footer /></div>;
}
